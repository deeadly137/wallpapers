# Contributing

Anyone can contribute, but here's how you do it:

1. Fork the repo

2. Add wallpapers to `Wallpapers/` — desktop ones go under `Wallpapers/Desktop/<category>/`, phone ones under `Wallpapers/Mobile/`

3. Name your files in lowercase kebab-case, e.g. `my-cool-wallpaper.png` (you can put the resolution at the end, like `my-cool-wallpaper-4k.png`)

4. Commit your changes

5. Create a new pull request on main branch

And then, repository maintainers are going to check if they match the wanted criterion:

- PNG or JPEG (JPEG preferred for photos, PNG for flat/gradient art)
- At least 1920x1080 for desktop, at least 1080x2340 for mobile

You don't have to touch the preview pages by hand — they are generated by `scripts/generate_previews.py` and CI keeps them up to date.
